#include "main.h"
/**
 *_abs computes the absolute value of integer
 *Return the value
 */
int _abs(int)
{
	if (int < 0)
	{
		int * -1;
		return (int);
	}
	else if (int == 0 || int > 0)
	{
		return (int);
	}
}
