#include "main.h"
/**
 *_abs computes the absolute value of integer
 *Return the value
 */
int _abs(int n)
{
	int n;

	if (n < 0)
	{
		n * -1;
		return (n);
	}
	else if (i == 0 || n > 0)
	{
		return (n);
	}
}
