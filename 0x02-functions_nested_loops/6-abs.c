#include "main.h"
/**
 *_abs computes the absolute value of integer
 *Return the value
 */
int _abs(int)
{
	int i;

	if (i < 0)
	{
		i * -1;
		return (i);
	}
	else if (i == 0 || i > 0)
	{
		return (i);
	}
}
